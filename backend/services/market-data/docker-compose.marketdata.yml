services:
  market-data-service:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      - APP_PORT=8080
      - RESOURCES_PATH=resources/
    networks:
      - brokerx-network
    restart: unless-stopped

networks:
  brokerx-network:
    driver: bridge
    external: true
